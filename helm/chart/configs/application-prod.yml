spring:
  application:
    name: sbm-declaration-services

esb:
  # ESB_SERVER is exported from global.overrides.esb.server in helm/values/live.yaml
  # and helm/values/dr.yaml.
  base-url: ${ESB_SERVER:http://esb.allianz.com.tr:12000}

token-management:
  # TODO(confirm): the production base URL of alz-token-management is still pending; the
  # TEST address below must be replaced before the first production run.
  base-url: https://int-sc-test-auth.allianz.com.tr

logging:
  level:
    tr.com.allianz.ysv.services: INFO
